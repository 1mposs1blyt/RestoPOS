{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Бэкенд — Node, а не браузер: убираем DOM из lib и добавляем типы node.
    "lib": ["ES2023"],
    "types": ["node"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "noEmit": true
  },
  "include": ["src"]
}
