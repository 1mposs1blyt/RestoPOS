/**
 * Маршруты прилавка.
 *
 * Роутера-библиотеки нет намеренно: терминал работает в киоске, URL не виден
 * и делиться им не с кем. Экранов пять, стек «назад» не нужен — из любого
 * служебного возвращаются на продажу.
 */
export type ServiceScreen = "shift" | "stoplist" | "refund";

export type Screen = "counter" | ServiceScreen;
