{
  // Корневой конфиг нужен редактору и запускам tsc из корня: он даёт
  // path-алиасы на воркспейс-пакеты. Реальная проверка типов идёт
  // по конфигам в каждом пакете (`pnpm -r typecheck`).
  "extends": "./tsconfig.base.json",
  "files": [],
  "include": []
}
