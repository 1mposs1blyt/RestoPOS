# Очередь задач автономного агента

Демон (`daemon.ps1`) берёт **первую** невыполненную задачу, выполняет её целиком,
прогоняет `pnpm verify` и коммитит. Одна задача за запуск.

Пометки: `[ ]` не сделано · `[x]` сделано · `[!]` задача неверна или невыполнима,
рядом строка почему.

Задачи писать **проверяемыми**: у каждой должно быть видно, чем доказывается,
что она выполнена. «Улучшить экран» — не задача, «привести экран оплаты
к раскладке экрана заказа и проверить замером, что нет целей меньше 44px» —
задача.

---

## Чистка после переезда печати в Rust

- [x] Убрать зависимость от `@tauri-apps/plugin-shell`. Фронт её больше
      не использует — печать на ККТ ушла в команду `atol_print_lines`
      (`src-tauri/src/fiscal/commands.rs`). Удалить пакет из
      `apps/desktop/package.json`, крейт `tauri-plugin-shell` из
      `src-tauri/Cargo.toml`. Проверка: `pnpm verify`, `cargo check --release`,
      в `apps/desktop/src` нет ни одного упоминания `plugin-shell`.

- [x] Убрать разрешение `shell:allow-execute` и блок `run-powershell`
      из `src-tauri/capabilities/default.json`. Оно осталось от прежней сборки
      и теперь ни к чему не привязано: выдавать из webview право запускать
      `powershell` с произвольными аргументами незачем. Проверка:
      `cargo check --release`, тестовая печать с экрана оборудования работает.
      Обновить абзац про это в `CLAUDE.md` — там сказано «строку стоит убрать».
      **Правки уже сделаны попутно с предыдущей задачей**: без снятого
      разрешения релиз не собирается вовсе («Permission shell:allow-execute
      not found»), так что блок из capabilities убран, абзац в `CLAUDE.md`
      переписан. Печать на живой ККТ проверена штатным ignored-тестом
      `fiscal::atol::live::тестовая_печать` — лента вышла.

## Интерфейс под iikoFront

Образец — `screens/orderscreen.tsx` и `screens/counterscreen.tsx`: плоские
панели, разделённые границей в пиксель, без внешних отступов; полоса функций
внизу; цель касания не меньше 44px по обеим сторонам.

- [x] Привести `screens/paymentscreen.tsx` к этой раскладке. Проверка:
      замером `getBoundingClientRect` на 1024x768 и 1366x768 нет ни одного
      интерактивного элемента меньше 44px и нет горизонтального скролла.
      Замер сделан не глазами, а `tools/measure-screen.mjs` (Chrome по CDP,
      три состояния экрана на обоих разрешениях) — им же меряются два
      следующих экрана. Полоса функций вынесена в общий
      `components/functionbar.tsx`: третьей копии не будет.

- [x] То же для `screens/cashscreen.tsx`. Три колонки (свод смены и свод ККТ,
      движения по ящику, операции клавиатурой), функции полосой внизу, отчёты
      в ней же — рядом с инкассацией «Закрыть смену» стоять не должна.
      Замерено `tools/measure-screen.mjs` потоком `cash`: четыре состояния
      на 1024x768 и 1366x768, целей меньше 44px и горизонтальной прокрутки нет.

- [x] То же для `screens/kitchenscreen.tsx`. Тикеты сеткой с переносом,
      станции колонкой справа со счётчиком, полоса функций внизу — только
      когда есть куда возвращаться. Читаемость с двух метров проверяется
      не глазами: в `tools/measure-screen.mjs` добавлен порог кегля внутри
      тикета (24px, главное — 30px), поток `kitchen` меряет четыре состояния
      на 1024x768 и 1366x768 — всё зелёное. Потоку нужен отдельный дев-сервер
      без узла, команда — в комментарии к потоку.

## Долги по коду

- [x] Тест на идемпотентность `fiscal_configure`: повторный вызов с теми же
      адресом и портом **не должен** пересобирать устройство, иначе на стенде
      обнуляется открытая смена. Сейчас это поведение держится только
      комментарием. Проверка: `cargo test --lib`.
      Тело команды вынесено в `configure(&FiscalState, ...)` — `tauri::State`
      в тесте не собрать. Четыре теста в `fiscal::commands::tests`: повтор
      настроек открытую смену не роняет, смена адреса и порта устройство
      пересобирают, род устройства с фронта не меняется.

- [!] Прокинуть `taxSystem` и `defaultVat` из карточки ККМ в чек возврата
      (`state/checkout.tsx`, ветка `refund`). Сейчас в продаже они берутся
      из карточки, а в возврате — по умолчанию, и документы расходятся.
      Проверка: `pnpm verify` плюс тест на состав чека возврата.
      **Прокидывать нечего: расхождения нет.** Ветка `refund` берёт оба
      значения из карточки ККМ теми же выражениями, что и продажа
      (`checkout.tsx:474` и `:481` против `:281` и `:285`), — и берёт
      с самого появления возврата (коммит `f6ae6f6`); в драйвере АТОЛ
      род документа на `tax_system` и ставку тоже не влияет.

## Данные стенда

- [x] Наполнить меню узла до размера, на котором видно поведение сетки:
      не меньше 30 позиций в 6 категориях, с длинными названиями. Заполнять
      **в БД напрямую** (`docker exec -i resto-db psql -U postgres -d resto-db`),
      а не сидом внутри сервера — код узла чужая зона. Проверка: `GET
      /api/v1/venues/.../menu` отдаёт их, у всех проставлен `prep_station_id`.
      Налито `tools/seed-menu-stand.sql` (идемпотентен, триггер ревизий
      не глушит — строка без ревизии не уедет в облако): 6 категорий
      (добавлены «Гарниры»), 38 позиций, четыре названия длиннее 40 знаков.
      Узел отдаёт их все, `prepStationId` не пуст ни у одной.

---

# Вторая очередь

Задачи первой очереди закрыты 25 августа. Ниже — то, что вскрылось по дороге
(первые две записаны в «Открытых вопросах» `docs/state.md`) и долги, названные
в `CLAUDE.md`.

## Дефекты

- [x] **Неизвестная станция должна работать по бумаге, а не копить тикеты.**
      На терминале, подключённом к узлу, меню приносит `prepStationId` вида
      `88888888-…-888888888881`, которого нет в `state/stations.tsx`. Позиция
      с таким идентификатором не попадает в `autoReadyStationIds`, уходит
      в `cooking` и висит там навсегда: отметить готовность некому — экрана
      у такой станции нет.

      Правило на этот случай в `CLAUDE.md` уже есть («станция без включённого
      экрана работает по бумаге, её позиции становятся `ready` сразу при
      отправке»), просто не применяется к незнакомым идентификаторам:
      авто-готовность считается от списка станций, а должна — от наличия
      вывода-экрана у этого идентификатора. Нет вывода — нет экрана — бумага.

      Это **не** заглушка под несуществующий эндпоинт и не переезд станций
      на узел: `data/stations-source.ts` писать не надо, маршрута под него
      у узла нет. Здесь чинится поведение при неизвестном идентификаторе,
      которое обязано быть определённым независимо от того, откуда он приехал.

      Отдельно: тикет такой станции показывается без имени («Монитор кухни»)
      и не считается в колонке станций справа — его видно только в «Все».
      Показывать в этом случае сам идентификатор: неизвестная станция должна
      быть видна, а не выглядеть отсутствующей.

      Проверка: тест в `state/orders-reducer.test.ts` — позиция со станцией,
      которой нет ни в списке станций, ни в выводах, после `order/send`
      получает `ready`; `pnpm verify`.
      Список перевёрнут: редьюсер получает `screenStationIds` (станции
      **с** экраном, считает `stationIdsWithScreen` в `shared-types` от выводов),
      и всё, чего в нём нет, работает по бумаге — обратным списком незнакомый
      идентификатор выразить нельзя. Кухонный экран показывает такую станцию
      укороченным с обеих сторон идентификатором (`888888…888881`: у станций
      одного заведения совпадает начало) и считает её тикеты в колонке справа.
      `pnpm verify` зелёный, 252 теста; Rust не трогался.

- [x] **Тест на состав чека возврата.** `receiptItems` для возврата собирается
      от текущего состава заказа и `orderTotals(...).total` — тем же путём, что
      и продажа, но под тестом этот путь только со стороны
      `lib/receipt-lines.ts`. Расхождение здесь — это документ, который ККТ
      отклонит посреди расчёта с гостем, и заметят его не в тестах.

      Проверка: тест, где заказ со скидкой и несколькими позициями даёт
      в возврате те же строки и ту же сумму, что и в продаже, а сумма строк
      сходится с суммой платежей до копейки; `pnpm verify`.
      Тест — `state/checkout-receipt.test.ts`, 10 проверок: заказ со скидкой
      суммой (её остаток и раздаётся построчно), сторнированная позиция в чек
      не идёт, цена берётся из позиции, а не из подорожавшего меню; платежи
      возврата собираются от записанных `Payment`, продажи — от черновиков,
      и совпадают; суммы сходятся с итогом заказа до копейки. Строки обеим
      веткам `checkout.tsx` теперь собирает один `buildReceiptItems`.
      Попутно: `receiptItems` не отсеивал позиции `split` — разделённое блюдо
      печаталось рядом со своими долями. Отсеивает.

## Гейт

- [x] **`pnpm verify:rust`.** `pnpm verify` не трогает Rust вовсе, и гейт
      остаётся зелёным при сломанном `cargo test` и не собирающемся релизе —
      так уже случалось, поломка прожила несколько коммитов (`CLAUDE.md`).
      Отдельный скрипт: `cargo test --lib` плюс `cargo check --release`,
      с добавлением `~/.cargo/bin` в `PATH` (в PATH оболочки cargo может
      не быть). В `pnpm verify` **не встраивать**: полминуты сборки Rust
      на каждую правку вёрстки — цена, которую перестанут платить, и гейт
      начнут обходить целиком.

      Проверка: `pnpm verify:rust` зелёный; заведомо сломанный тест роняет
      его с ненулевым кодом; в `CLAUDE.md` таблица трёх проверок ссылается
      на команду, а не на голый `cargo`.
      Скрипт — `scripts/verify-rust.mjs`: `cargo test --lib`, затем
      `cargo check --release` в `apps/desktop/src-tauri`. `~/.cargo/bin`
      добавляется в PATH скриптом (ключ ищется без учёта регистра — под Windows
      он `Path`), а cargo ищется по PATH явно: разрешения по PATHEXT `spawn`
      не делает. Зелёный (49 тестов, релиз собирается); заведомо сломанный тест
      уронил его кодом 101. В `pnpm verify` не встроен — причина в шапке
      скрипта и в `CLAUDE.md`.

## Контракт

- [x] **Довести генератор тарифов до конца.** Лестница описана в
      `contracts/contract.json` и проверяется там строже прав, но в
      сгенерированные файлы **не попадает**, и значения продублированы руками:
      `PlanCode` / `FeatureCode` в `packages/shared-types/src/common.ts`
      и `PLAN_FEATURES` в `apps/desktop/src/app/entitlements.tsx`.
      Это ровно та ситуация, против которой написан абзац про матрицу прав,
      только про деньги: разъедется — либо отдадим оплаченный модуль бесплатно,
      либо отберём у клиента оплаченное.

      Научить `scripts/build-contract.mjs` писать `planCodes`, `featureCodes`,
      `planFeatures` и `planQuotas` в оба сгенерированных файла, а обе ручные
      копии заменить на импорт из сгенерированного.

      **Сид `plans` / `plan_features` в `schema.sql` не трогать** — чужая зона.
      Третья копия остаётся; если она разошлась с контрактом, описать
      расхождение в `docs/state.md`, а не править.

      Проверка: `pnpm contracts:check` зелёный, `pnpm verify` зелёный,
      в `common.ts` и `entitlements.tsx` не осталось литералов тарифов и фич.
      Генератор пишет `CONTRACT_PLAN_CODES` / `CONTRACT_FEATURE_CODES` /
      `CONTRACT_PLAN_FEATURES` / `CONTRACT_PLAN_QUOTAS` в TypeScript и
      `PlanCodes` / `FeatureCodes` / `PlanFeatures` / `PlanQuotas` плюс
      `PlanHasFeature` и `QuotasOf` — у узла. Копий на фронте оказалось
      больше трёх: кроме названных, лестницу дублировали `featuresOfPlan`
      в `data/session-source.ts`, списки `PLANS` в дев-панели и на сервисном
      экране и `ALL_FEATURES` в тесте навигации — все берут контракт.
      `FEATURE_LABELS` из `Partial<Record>` стал полным `Record`: фича,
      добавленная в контракт и забытая в подписях, теперь роняет сборку.
      Сид в `schema.sql` не тронут и с контрактом сходится (сверено).
      `pnpm verify` зелёный, 262 теста; `dotnet build` узла в сторону —
      0 ошибок, 0 предупреждений (в шапку C# добавлен `#nullable enable`:
      в файле `*.generated.cs` компилятор гасит nullable-контекст сам).

## Интерфейс

Образец и правила замера — в первой очереди. Замер: `node tools/measure-screen.mjs
<поток> 1024x768,1366x768`, новый экран требует нового потока в `flows`.

- [x] **`screens/tablescheme.tsx` и зал.** Схема рисуется 1:1 и в долях холста —
      этого **не менять** (причина в `CLAUDE.md`, промежуточный вариант
      с масштабированием уже пробовали и он оказался хуже). Приводится к общей
      раскладке обвязка: полоса функций внизу, плоские панели, границы в пиксель.
      Проверка: замер потоком `hall` на обоих разрешениях — нет целей меньше
      44px и нет горизонтальной прокрутки; столы после правки остаются
      на прежних местах.
      Холст не тронут — координаты в долях те же, столы на своих местах.
      Действия ушли вниз: «Назад», «Обновить», «Конструктор зала», а в режиме
      конструктора — три клавиши добавления и зелёное «Сохранить расстановку».
      Шапка осталась под состояние (столов N, дубли номеров, ошибка зала,
      «занятость не обновляется»), кнопку перечитать зала она больше не держит.
      Замер потоком `hall` (новый, ему нужен дев-сервер без узла — команда
      в комментарии): пять состояний на 1024x768 и 1366x768, зелено.
      Попутно вскрылось замером: кнопка удаления стола висела за углом стола
      и у крайних столов уезжала под обрез холста — удалить такой стол было
      нечем. Теперь она соседка стола на холсте, а не его часть, и прижимается
      к границе так же, как сам стол.

- [ ] **`screens/reportsscreen.tsx`.** То же. Отдельно проверить, что таблицы
      отчётов прокручиваются внутри своей панели, а не тянут страницу вбок:
      горизонтальная прокрутка страницы — нарушение, прокрутка внутри
      контейнера — нет.

## Сборка

- [ ] **Пересобрать обе кассы и проверить запуск.** Собранные бинарники
      от 03:20 — старше всех ночных правок, так что проверяют они уже не тот
      код, что в репозитории. Пересобрать релиз и отладочную (`--debug`),
      запустить обе, убедиться, что окно поднимается и экран оплаты открывается.

      Перед сборкой погасить кассу (`Get-Process desktop | Stop-Process -Force`)
      — cargo не может перезаписать занятый `desktop.exe`. MSI собирать
      не обязательно; если WiX упадёт, хватит `--bundles nsis`.

      Проверка: обе `desktop.exe` новее последнего коммита, обе запускаются.
